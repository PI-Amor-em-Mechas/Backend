mediapipe>=0.10.14
opencv-python>=4.10.0
opencv-contrib-python>=4.10.0
numpy>=1.24.0
flask>=3.0.0
flask-socketio>=5.3.0
simple-websocket>=1.0.0
vosk>=0.3.45
gevent>=24.0.0
gevent-websocket>=0.10.1
pyttsx3>=2.90
piper-tts>=1.2.0
PyMySQL>=1.1.0
# Voice biometrics (speaker verification) — 2nd factor after face recognition.
# torch is brought in as a transitive dep; pin CPU build via:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# webrtcvad-wheels provides prebuilt Windows wheels (avoids MSVC build tools);
# it ships the same `webrtcvad` module that resemblyzer imports.
webrtcvad-wheels>=2.0.14
resemblyzer>=0.1.4
torch>=2.0.0
librosa>=0.10.0